# new_insight_env — PyTorch 2.x stack for CleverHansRegression / INSightR-Net
#
# Tested Python: 3.10.16
#
# === Conda setup (install PyTorch via conda, then pip for the rest) ===
#   conda create -n new_insight_env python=3.10
#   conda activate new_insight_env
#   conda install pytorch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 pytorch-cuda=11.8 -c pytorch -c nvidia
#   pip install -r requirements.txt
#
# PyTorch stack (conda — listed here for reference, not installed by this file):
#   python==3.10.16
#   pytorch==2.5.1
#   torchvision==0.20.1
#   torchaudio==2.5.1
#   pytorch-cuda==11.8
#   triton==3.1.0          # pulled in with torch (pip)

# --- Training / Lightning ---
pytorch-lightning==2.1.4
torchmetrics==1.7.1
mlflow==2.20.3
enforce-typing==1.0.0.post1
dataclasses-json==0.6.7

# --- Data / images ---
opencv-python==4.11.0.86
imageio==2.28.0
Augmentor==0.2.12
Pillow==9.4.0

# --- Numerics / tables ---
numpy==1.22.4
pandas==1.4.2
scipy==1.8.1

# --- ML / stats / plots ---
scikit-learn==1.3.2
scikit-image==0.22.0
matplotlib==3.7.3
seaborn==0.12.2
statsmodels==0.14.4
joblib==1.5.0

# --- Utils ---
tqdm==4.67.1
PyYAML==6.0
urllib3==2.3.0

# --- Figure export (scripts/convert_svg_to_pdf.py) ---
reportlab==4.5.1
svglib==1.6.0
lxml==6.1.1
